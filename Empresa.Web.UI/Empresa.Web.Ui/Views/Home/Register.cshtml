@using System.Web.Mvc.Html
@{
    // CRUCIAL: Impede o uso de qualquer layout mestre
    Layout = null;
}
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Criar Nova Conta</title>

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <style>
        body {
            /* Fundo azul escuro e centralização */
            background-color: #2b6cb0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .register-card {
            /* Estilo do painel branco */
            background-color: white;
            padding: 25px 40px;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            max-width: 450px; /* Mais largo para caber os campos */
            width: 100%;
        }
        /* Estilos adicionais para simular o visual de modal */
        .modal-header {
            border-bottom: 0;
            padding: 0 0 1rem 0;
            margin-bottom: 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .modal-title {
            font-size: 1.5rem;
        }

        .modal-footer {
            border-top: 0;
            padding: 1rem 0 0 0;
            display: flex;
            justify-content: space-between;
        }

        /* 🎨 Estilo para mensagens de erro */
        .alert {
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>

    <div class="register-card">

        <div class="modal-header">
            <h5 class="modal-title">
                <i class="fas fa-user-plus text-primary mr-2"></i> Criar Nova Conta
            </h5>
            <a href="@Url.Action("Login", "Home")" class="close" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </a>
        </div>

        @* Exibe mensagens de erro *@
        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle"></i> @ViewBag.ErrorMessage
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }

        @using (Html.BeginForm("Register", "Home", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.AntiForgeryToken()

            <div class="modal-body p-0">

                <div class="form-group">
                    <label for="NomeCompleto">Nome Completo:</label>
                    <input type="text" class="form-control" id="NomeCompleto" name="NomeCompleto" required>
                </div>

                <div class="form-group">
                    <label for="Email">E-mail:</label>
                    <input type="email" class="form-control" id="Email" name="Email" required>
                </div>

                <div class="form-group">
                    <label for="NomeUsuario">Nome de Usuário:</label>
                    <input type="text" class="form-control" id="NomeUsuario" name="NomeUsuario" required>
                </div>

                <div class="form-group">
                    <label for="Senha">Senha:</label>
                    <input type="password" class="form-control" id="Senha" name="Senha" required>
                </div>

                <div class="form-group">
                    <label for="ConfirmarSenha">Confirmar Senha:</label>
                    <input type="password" class="form-control" id="ConfirmarSenha" name="ConfirmarSenha" required>
                </div>

                @* 🔧 CORREÇÃO: Agora envia números (1 ou 2) *@
                <div class="form-group">
                    <label for="TipoUsuario">Tipo de Usuário:</label>
                    <select class="form-control" id="TipoUsuario" name="TipoUsuario" required>
                        <option value="">Selecione o tipo</option>
                        <option value="2" selected>Colaborador</option>
                        <option value="1">Administrador</option>
                    </select>
                </div>

            </div>

            <div class="modal-footer">
                <a href="@Url.Action("Login", "Home")" class="btn btn-secondary">
                    <i class="fas fa-times-circle"></i> Cancelar
                </a>

                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-user-plus"></i> Criar Conta
                </button>
            </div>
        }

    </div>

    @* Scripts do Bootstrap *@
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>